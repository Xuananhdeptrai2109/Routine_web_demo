// Orders data layer
// Note: All mock orders have been migrated to MySQL Routine DB.
// Data is dynamically retrieved via API (/api/v1/orders).

export const seedOrders = [];

export const dashboardTotals = {
  totalOrders: 0,
  totalCustomers: 0,
  revenue: 0,
};

export const orders = [];
